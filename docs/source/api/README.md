# Generated API reference

This directory is written entirely by Sphinx's `autosummary` extension —
nothing here is hand-edited. It's generated from the `:toctree: api`
directive in `../api.md`, and regenerated (incrementally) on every docs
build.

Everything in this directory except this README is gitignored.

# Generated example gallery

This directory is written entirely by `sphinx-gallery` — nothing here is
hand-edited. It's generated from the scripts in `../../../examples/` (see
`gallery_dirs`/`examples_dirs` in `../conf.py`), and regenerated
(incrementally) on every docs build.

Everything in this directory except this README is gitignored.
